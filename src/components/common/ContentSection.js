import React from 'react';
import { View } from 'react-native';
import styled from 'styled-components/native';

export default (ContentSection = styled(View)`
  justify-content: center;
  align-content: center;
  flex: 1;
`);
